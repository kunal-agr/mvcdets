services:
  db:
    image: postgres:15
    container_name: mypostgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mvcdetsdb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db-init:/docker-entrypoint-initdb.d
    networks:
      - mynet

  app:
    build: .
    image: daily-expense-tracker
    container_name: daily-expense-tracker
    depends_on:
      - db
    environment:
      DB_URL: jdbc:postgresql://db:5432/mvcdetsdb
      DB_USER: postgres
      DB_PASS: password
    ports:
      - "8080:8080"
    networks:
      - mynet

volumes:
  pgdata:

networks:
  mynet:
    driver: bridge
